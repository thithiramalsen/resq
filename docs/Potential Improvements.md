A mini Map displayed on the SOS creation page that displays users their location, and allows them to drop the pin on somewhere else. 
On the SOS display page, a hyperlink of google map instead of just the plain text. That way, responding parties can click on the hyperlink and be redirected to the google maps for easier navigation during a traversal.
Same 2 suggestions for report location
Current location on map is wrong or buggy
In the home page, recent alerts could use displaying submitted time
THe location detection is wrong on emergency request and various other components. could be an ISP related issue 
SUbmit emergency request page and SOS emergency tracker needs a back button
